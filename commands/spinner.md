---
description: Show words you're learning in the Claude Code spinner tips (on, off, status)
argument-hint: "[on|off|status]"
allowed-tools: Bash(${CLAUDE_PLUGIN_ROOT}/scripts/cli.sh *)
---

Run `${CLAUDE_PLUGIN_ROOT}/scripts/cli.sh spinner $ARGUMENTS` and show the user its output verbatim. Do not add commentary.
