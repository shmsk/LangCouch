---
description: Switch LangCouch target language or list available ones
argument-hint: [code]
allowed-tools: Bash(${CLAUDE_PLUGIN_ROOT}/scripts/cli.sh *)
---

Run `${CLAUDE_PLUGIN_ROOT}/scripts/cli.sh lang $ARGUMENTS` and show the user its output verbatim. If it errors, show the error message as-is.
