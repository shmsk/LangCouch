---
description: Show LangCouch level and per-language progress
allowed-tools: Bash(${CLAUDE_PLUGIN_ROOT}/scripts/cli.sh *)
---

Run `${CLAUDE_PLUGIN_ROOT}/scripts/cli.sh status` and show the user its output verbatim in a code block. Do not add commentary.
